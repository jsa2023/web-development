/**
 * tells the code if this thing is true then simply run more code
 */


const shouldSayHi = true;

// console.log(Boolean(shouldSayHi));

// console.log(shouldSayHi === true);
if(Boolean(shouldSayHi)){
    console.log("Hi there bro");
}